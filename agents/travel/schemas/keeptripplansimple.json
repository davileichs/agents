{
  "description": "Connect to Keep Trip Plan Simple (KTPS) and fetch trip data. It has all information about users trips",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "save_token",
          "list_trips",
          "chat"
        ],
        "description": "list_trips: fetch from token URL - ALWAYS use first when user asks about trips, then show the list. chat: use to talk with an AI agent about a specific trip."
      },
      "token": {
        "type": "string",
        "description": "Token ID to be saved to access KTPS trips"
      },
      "trip_id": {
        "type": "string",
        "description": "Trip ID from the trips list. Can be provided once to chat, and will be saved for subsequent chat requests."
      },
      "message": {
        "type": "string",
        "description": "User message string to send to the chat agent."
      }
    },
    "required": [
      "action"
    ]
  }
}