{
  "type": "object",
  "description": "Get a map of a location in Final Fantasy XIV with optional coordinate marking",
  "properties": {
    "location_name": {
      "type": "string",
      "description": "Name of the location to get a map for"
    },
    "mark_coordinates": {
      "type": "boolean",
      "description": "Whether to mark coordinates on the map (optional)",
      "default": false
    },
    "x_coordinate": {
      "type": "number",
      "description": "X world coordinate to mark on the map (required if mark_coordinates is true)"
    },
    "y_coordinate": {
      "type": "number", 
      "description": "Y world coordinate to mark on the map (required if mark_coordinates is true)"
    }
  },
  "required": ["location_name"]
}
