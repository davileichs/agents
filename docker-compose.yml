services:
  agent-api:
    build: .
    container_name: agent-service
    ports:
      - "8000:8080"
    env_file:
      - .env
    volumes:
      # Mount the agents directory so agents can be updated without rebuilding
      - ./agents:/app/agents
      - ./app:/app/app
    restart: unless-stopped
